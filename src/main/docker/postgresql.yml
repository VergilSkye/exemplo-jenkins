version: '2'
services:
  exemplo-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/exemplo/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=exemplo
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
